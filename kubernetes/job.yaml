apiVersion: batch/v1
kind: Job
metadata:
  name: hellojob
spec:
  template: 
    metadata:
      name: jobtemp
    spec:
      restartPolicy: "Always"
      containers:
        - image: httpd:latest
          name: httpdjob
          command: 
            - sleep
            - 1d
